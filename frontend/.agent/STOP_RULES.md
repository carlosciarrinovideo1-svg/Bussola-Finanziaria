# STOP RULES

L'agente si interrompe solo se:

- è necessaria una decisione funzionale;
- è richiesta una modifica architetturale;
- esiste un conflitto Git;
- la build non è correggibile automaticamente;
- manca un'informazione indispensabile.

Ogni altro problema deve essere risolto autonomamente.
